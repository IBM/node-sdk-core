import { PackageJson } from './index';
export declare function stripLeadingSlash(string: string): string;
export declare function replaceString(string: string, outDir: string): string;
export declare function modifyPackageJson(packageJson: PackageJson, outDir: string): PackageJson;
export declare function shouldIncludeFile(entry: string, outDir: string): boolean;
/**
 * Gets list of files to include in the build folder.
 *
 * List of files will include files not ignored by .npmignore (if exists)
 * and always README, LICEN[CS]E, and CHANGELOG (if they exist).
 *
 * @param path path of directory to get files out of
 */
export declare function getNonSrcFiles(path: string, outDir?: string): string[];
/**
 * Function to load the tsconfig.json file, which allows for both comments as well
 * as for trailing commas.
 * @param path
 */
export declare function parseTsConfig(path: string): any;
//# sourceMappingURL=utils.d.ts.map
export interface Command {
    describe(): void;
    execute(): Promise<number>;
}
export declare class ExecCommand implements Command {
    command: string;
    args: string[];
    cwd: string;
    constructor(cwd: string, command: string, args?: string[]);
    describe(): void;
    execute(): Promise<number>;
}
export declare class NpmCommand extends ExecCommand {
    constructor(cwd: string, command: string[] | string);
}
export declare class NpmRunCommand extends NpmCommand {
    constructor(cwd: string, command: string, args?: string[]);
}
export declare class CopyCommand implements Command {
    file: string;
    src: string;
    dest: string;
    constructor(src: string, dest: string, file: string);
    describe(): void;
    execute(): Promise<number>;
}
//# sourceMappingURL=command.d.ts.map
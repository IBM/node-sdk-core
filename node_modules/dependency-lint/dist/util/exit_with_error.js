(function() {
  var colors, exitWithError, util;

  colors = require('colors/safe');

  util = require('util');

  exitWithError = function(err) {
    var message;
    message = (err != null ? err.stack : void 0) || util.format(err);
    console.error(colors.red(message));
    return process.exit(1);
  };

  module.exports = exitWithError;

}).call(this);
